{
  "name": "viet-coach",
  "version": "1.0.0",
  "description": "Vietnamese Language Coach - Real-time voice coaching app",
  "scripts": {
    "dev": "pnpm --parallel --filter api --filter web run dev",
    "dev:api": "pnpm --filter api run dev",
    "dev:web": "pnpm --filter web run dev",
    "dev:voice": "pnpm --filter voice run dev",
    "build": "pnpm --filter @repo/data-ops run build",
    "build:web": "pnpm --filter web run build"
  },
  "keywords": ["vietnamese", "language-learning", "voice-coach"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.15.19",
    "typescript": "^5.9.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "workerd"
    ]
  }
}
